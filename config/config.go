package config

// load the env's to the configuration
func loadConfig(path string)  {
	
}

// take a path as an parameter if the path string is empty then it search 
// for the envs on the root .env file return a pointer to configuration env's 
// location